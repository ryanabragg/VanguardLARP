import styled from 'styled-components';

const Navigation = styled.nav`
  position: absolute;
  top: 20px;
  right: 20px;
  .icon {
    margin: 0px;
    padding: 0px;
  }
`;

export default Navigation;
